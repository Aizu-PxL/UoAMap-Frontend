# UoAMap API契約(ドラフト v0)

最終更新: 2026-07-18

フロントエンドが期待するバックエンドAPIの契約。バックエンドはこれに合わせて実装する。
変更はフロント・バックエンド双方合意の上でこのファイルを先に更新する。

方針(SPEC.md 4章・5章より):
- バックエンドの役割は**イベント・場所対応・QR対応表の管理と配信**のみ
- 経路計算・検索はフロント側で行うため、**経路APIと検索APIは存在しない**
- レスポンスはすべてJSON・UTF-8。認証なし(公開読み取り専用API)
- 書き込み系(登録・編集)は管理用であり、この契約の対象外(バックエンド内部の自由。スプレッドシート取込等でも良い)

## エンドポイント

### GET /api/events

イベント全件を返す。フロントは起動時に1回取得し、以後クライアント側で検索・絞り込みする。

```jsonc
{
  "events": [
    {
      "id": "L1",                          // 公式タイムテーブルの記号
      "title": "入試説明会",
      "description": "……",
      "placeId": "LH_lecture_large",       // フロントのPlace ID(SVG由来レジストリと一致必須)
      "tags": ["briefing"],
      "timeSlots": [                        // 複数回開催は複数要素
        { "start": "2026-08-01T10:30:00+09:00", "end": "2026-08-01T11:00:00+09:00" },
        { "start": "2026-08-01T12:00:00+09:00", "end": "2026-08-01T12:30:00+09:00" }
      ]
    }
  ]
}
```

### GET /api/tags

タグ(検索フィルタのチップ)一覧。表示順で返す。

```jsonc
{
  "tags": [
    { "id": "briefing", "label": "説明会" },
    { "id": "openlab",  "label": "研究室公開" },
    { "id": "trial",    "label": "体験授業" }
  ]
}
```

### GET /api/qr/{qrId}

QR IDを地点に解決する。QRの貼り替え・再割当はこの表の更新だけで完結する。

```jsonc
// 200
{ "qrId": "Q012", "placeId": "RQ1F_room_e1_seminar10_133" }
// 404: 未知のqrId → フロントはエラー表示(地図はデフォルト表示)
```

種別(固定/可変)や設置場所メモはバックエンド内部の管理項目であり、レスポンスには含めない。

## 共通事項

- `placeId` の語彙はフロントのPlaceレジストリ(SVGから生成)が正。バックエンドに登録する際はフロントが書き出す一覧(将来 `places.json` として提供)から選ぶ
- イベントデータは年度ごとに総入れ替え。IDは年度内でユニークであれば良い
- キャッシュ: `/api/events`・`/api/tags` は数分程度のキャッシュ可。`/api/qr/{qrId}` は当日貼り替え運用があるためキャッシュ短め(60s以下)を推奨
- CORS: 本番フロントのオリジンを許可(ホスティング先確定後に記載)

## 未決事項

- [ ] ベースURL(ホスティング先・バックエンドの技術選定後に確定)
- [ ] 研究室公開の個別イベントID採番(SPEC.md 7章参照)が決まったらexampleを追記
