# 20 地図注目とイベントバッジの回帰修正

## GOAL

目的地を保持した初回QR解決後と、イベント詳細の「ここへ行く」後に、地図を現在地へ確実に注目させる。イベントピンから詳細を開いたときは現在の地図表示を動かさず、イベントバッジを白地＋カテゴリ色の表示へ戻す。淡色カテゴリでもキーボードフォーカスを明確にし、イベント詳細の戻る導線を実際の遷移先に合わせる。

## 参照

- `docs/SPEC.md` §3.1、§3.2、§5.2
- `docs/STATUS.md`
- `docs/WORKFLOW.md`
- `docs/tasks/18-mobile-map-ux-adjustments.md`

## やること

- [x] SVG切替中の古い地図で注目要求を処理済みにせず、対象シート読込後に現在地へ注目する。
- [x] イベントピンから詳細を開く際に一時的な地図注目状態を保持し、目的地への再フォーカスを防ぐ。
- [x] イベントバッジを白地、カテゴリ色の縁・人型グリフへ変更する。
- [x] 淡色カテゴリでも濃色のキーボードフォーカスリングを維持する。
- [x] イベント詳細の戻るリンクを「リストに戻る」へ変更する。
- [x] 関連テスト、検証ゲート、幅402pxブラウザ確認、独立レビューを完了する。

## SCOPE

- `docs/STATUS.md`
- `docs/tasks/20-map-focus-event-marker-regressions.md`
- `src/features/map/MapCanvas.tsx`
- `src/features/events/EventDetail.tsx`
- `src/styles/global.css`

上記以外、Figma、依存、SVG、Routeグラフ、Repository/API契約は変更しない。

## 受入基準

- [x] 目的地付き初回QR `/q/Q001?to=M21` が `/?to=M21&at=main_node_11` へ正規化され、22svhで現在地へ注目する。
- [x] 現在地へ注目した経路表示中にイベントピンから詳細を開いても、地図のviewBoxが変わらず、シートだけが82svhへ上がる。
- [x] 現在地ありのイベント詳細で「ここへ行く」を押すと、22svhで現在地へ注目して経路を表示する。
- [x] イベントバッジの面が白、縁と人型がカテゴリ色で表示される。
- [x] キーボードフォーカス時も濃色外周が消えず、通常時より明確なフォーカスリングになる。
- [x] 正式IDと内部keyのイベント詳細で「リストに戻る」を表示し、既存クエリを保持して `/events` へ戻る。
- [x] `bun run verify:all` と `git diff --check` がPASSし、122 Place / 87 QR / 60 Event、347 nodes / 444 edgesを維持する。
- [x] 幅402pxの関連URLでconsole errorが0件である。
- [x] 会話履歴なしの読み取り専用レビューで指摘なし、または初回指摘を1回の修正・再検証・再レビューで解消する。

## DELIVERABLE

変更ファイル、受入結果、検証・ブラウザ・独立レビュー結果、残存リスクを簡潔に報告する。git履歴を変更しない。
