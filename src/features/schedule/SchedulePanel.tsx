export function SchedulePanel() {
  return (
    <div className="schedule-panel">
      <h2>タイムスケジュール</h2>
      {/* v1は公式PDFの画像表示(SPEC.md 3.6)。画像素材の組み込みはステップ6 */}
      <p>タイムスケジュールは準備中です。</p>
    </div>
  );
}
